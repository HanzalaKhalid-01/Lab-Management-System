<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - American Diagnostic LMS</title>

    <!-- Favicon (Logo on Browser Tab) -->
    <link rel="icon" type="image/png" href="~/images/logo.png" sizes="32x32">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- DataTables CSS -->
    <link href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" rel="stylesheet" />

    <!-- Select2 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />

    <!-- Your CSS -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>

@* <style>
    /* Glass-style transparent navbar */
    .custom-navbar {
        background-color: rgba(255, 255, 255, 0.2); /* semi-transparent */
        backdrop-filter: blur(10px); /* glass blur effect */
        -webkit-backdrop-filter: blur(10px); /* Safari support */
        padding: 0.5rem 1rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1); /* subtle shadow */
        transition: background-color 0.3s ease;
        z-index: 1030;
    }

        /* On scroll: make slightly more opaque for readability */
        .custom-navbar.scrolled {
            background-color: rgba(255, 255, 255, 0.85);
        }

        /* Navbar links */
        .custom-navbar .nav-link {
            color: #1a1a1a;
            transition: color 0.2s ease;
        }

            .custom-navbar .nav-link:hover {
                color: #0d6efd; /* Bootstrap primary hover */
            }

        /* Dropdown menu */
        .custom-navbar .dropdown-menu {
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            background-color: rgba(255, 255, 255, 0.9);
        }

        /* Brand logo adjustment */
        .custom-navbar .navbar-brand img {
            max-height: 80px;
        }

</style> *@

<body>
    <header>
        <nav class="navbar navbar-expand-sm sticky-top custom-navbar">
            <div class="container-fluid d-flex justify-content-between align-items-center">

                <!-- Brand (Left) -->
                <a class="navbar-brand" href="@Url.Action("Index","Home")">
                    <img src="~/images/logo.png" alt="New American Diagnostic Center"
                         class="d-inline-block align-text-top"
                         style="height:80px; width:auto;">
                </a>

                <!-- Main Links -->
                <div class="collapse navbar-collapse">
                    <ul class="navbar-nav ms-auto align-items-center">
                        <li class="nav-item">
                            <a class="nav-link fw-bold" href="@Url.Action("DailyClosing","Bills")">Daily Closing</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link fw-bold dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Tests</a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="@Url.Action("Create", "Tests")">Create Test</a></li>
                                <li><a class="dropdown-item" href="@Url.Action("Index", "Tests")">Tests List</a></li>
                            </ul>
                        </li>
                        <li class="nav-item"><a class="nav-link fw-bold" href="@Url.Action("CreateFullBill","Bills")">Create Bill</a></li>
                        <li class="nav-item"><a class="nav-link fw-bold" href="@Url.Action("Index","Bills")">Bill List</a></li>
                        <li class="nav-item"><a class="nav-link fw-bold" href="@Url.Action("PatientCredits","Bills")">Credits</a></li>
                    </ul>

                    <!-- Right Side Contact -->
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Privacy","Home")">Contact Dev</a>
                        </li>
                    </ul>
                </div>

                <!-- Toggler for small screens -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse">
                    <span class="navbar-toggler-icon"></span>
                </button>

            </div>
        </nav>

    </header>

    <!-- Body -->
    <div class="container-fluid">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <!-- Footer -->
    <footer class="border-top footer text-muted">
        <div class="container mt-2">
            &copy; 2026 - American Diagnostic Centre
        </div>
    </footer>

    <!-- jQuery (must be first) -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- DataTables JS -->
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>

    <!-- Select2 JS -->
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

    <!-- Your JS -->
    <script src="~/js/site.js" asp-append-version="true"></script>

    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>
@* 
<script>
    window.addEventListener('scroll', function () {
        const navbar = document.querySelector('.custom-navbar');
        if (window.scrollY > 50) {
            navbar.classList.add('scrolled');
        } else {
            navbar.classList.remove('scrolled');
        }
    });
</script> *@
